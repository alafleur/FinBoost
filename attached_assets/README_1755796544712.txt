FinBoost Hero Patch (frameless + tags + ticket + mobile order)
-----------------------------------------------------------------
Drop this file into your repo at:
  client/src/components/HeroLearnToEarn.tsx

It preserves:
- single component for desktop/mobile
- crisp screenshots via width-based srcSet/sizes (240/480/304/608)
- no scale() on the image
- headline first on mobile, screenshot second

The right side matches the earlier iteration (angled blue accent, subject tags, ticket overlay).
