--- a/client/src/pages/HomeV3.tsx
+++ b/client/src/pages/HomeV3.tsx
@@
-                <div
-                  className="relative w-64 lg:w-80 bg-gradient-to-b from-slate-800 to-slate-900 rounded-[2.5rem] lg:rounded-[3rem] p-2 shadow-xl lg:shadow-2xl shadow-slate-900/50"
-                  style={{ aspectRatio: 1 / imgRatio }}  // height adapts to maintain image proportions
-                >
+                {/* IMPORTANT: Tailwind cannot see template-literal classes. Use LITERAL pixel widths so purge works. */}
+                <div
+                  className="relative w-[240px] lg:w-[304px] bg-gradient-to-b from-slate-800 to-slate-900 rounded-[2.5rem] lg:rounded-[3rem] p-2 shadow-xl lg:shadow-2xl shadow-slate-900/50"
+                  style={{ aspectRatio: 1 / imgRatio }}
+                >
@@
-                        // ✅ Tell the browser the CSS width of the screen at each breakpoint
-                        sizes="(min-width: 1024px) 304px, 240px"
+                        /* Tell the browser the CSS width of the screen at each breakpoint (MUST stay literal) */
+                        sizes="(min-width: 1024px) 304px, 240px"
@@
-                  <div className="absolute bottom-1 lg:bottom-2 left-1/2 transform -translate-x-1/2 w-24 lg:w-32 h-1 bg-white/30 rounded-full"></div>
+                  <div className="absolute bottom-1 lg:bottom-2 left-1/2 transform -translate-x-1/2 w-24 lg:w-32 h-1 bg-white/30 rounded-full"></div>
+                </div>
+              </motion.div>
+
+              {/* REMOVE the small 'Learn to earn' pill above the H1 (if rendered by this page).
+                 If that badge comes from a separate Hero component, delete it there,
+                 or hide it by adding 'hidden' to the pill container. */}
