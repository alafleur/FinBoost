The blank white screen you're seeing on the dashboard (as shown in the screenshot) typically indicates a **JavaScript error breaking the React rendering process**. Since this started after the ProfileDrawer integration, here's a step-by-step troubleshooting path to identify and fix the issue:

---

## 🧪 Step 1: **Check Browser Console for Errors**

1. Press `F12` or `Ctrl+Shift+I` to open Developer Tools.
2. Go to the **Console** tab.
3. Look for red errors — likely causes:

   * `"Cannot read properties of undefined"` (likely `useContext` misconfig)
   * `"Element type is invalid"` (misuse of JSX or context)
   * Import/export mismatch in `useProfileDrawer.tsx`

Please copy-paste any error(s) shown here for a faster diagnosis.

---

## 🧪 Step 2: **Check for Common Mistakes in Recent Edits**

### 🔁 **App.tsx**

* Confirm the provider is correctly wrapped:

  ```tsx
  <ProfileDrawerProvider>
    <AppRoutes />
  </ProfileDrawerProvider>
  ```

### 🔁 **useProfileDrawer.tsx**

* `createContext(null)` used? Should type it properly.
* Ensure the exported hook is used **inside** the provider only:

  ```tsx
  const context = useContext(ProfileDrawerContext);
  if (!context) {
    throw new Error("useProfileDrawer must be used within a ProfileDrawerProvider");
  }
  ```

### 🔁 **ProfileDrawer.tsx**

* Confirm you're not referencing undefined `user` or `onUserUpdate`
* Ensure correct default props if optional

---

## 🧪 Step 3: **Temporarily Comment Drawer Usage**

If you're blocked, try commenting out just the new parts:

In `Dashboard.tsx`:

```tsx
// <ProfileDrawer user={user} onUserUpdate={fetchUserData} />
```

In `RewardsSummary.tsx`:

```tsx
// const { openProfileDrawer } = useProfileDrawer();
// onClick={() => openProfileDrawer('paypal')}
```

If the app loads after commenting those, the problem is **100% isolated to ProfileDrawer integration**.

---

## ✅ Likely Fixes Based on Your Summary

* **Fix 1**: The `ProfileDrawerProvider` might not be wrapped properly around `Dashboard` or `RewardsSummary`, making `useContext(ProfileDrawerContext)` undefined.
* **Fix 2**: JSX/Type error in `ProfileDrawer.tsx` or `useProfileDrawer.tsx`
* **Fix 3**: Mismatch in expected prop types (`user`, `onUserUpdate`) when rendering `ProfileDrawer`

---

## 🛠 What to Send Me

Please send:

1. The **exact error from browser console**
2. Your full `useProfileDrawer.tsx` file
3. The `App.tsx` file (just the wrapping)
4. The JSX code where you call `<ProfileDrawer />`

Once I see that, I’ll provide an exact patch.

Want me to draft a try/catch fallback or a minimal `ProfileDrawer` skeleton to prevent this in the meantime?
